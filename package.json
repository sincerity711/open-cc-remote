{
  "name": "open-cc-remote",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "bun run --filter='*' typecheck",
    "test": "bun test",
    "lint": "echo 'lint todo'"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
